import { AnimatedName } from "@/app/blog/animated-name.tsx";

export const metadata = {
  title: "Work",
  alternates: {
    canonical: "/work",
  },
};

export function Small({ children }) {
  return (
    <small className="-mt-3 mb-3 flex text-sm text-gray-500">{children}</small>
  );
}

# Work

<AnimatedName />

On a mission to build products developers

## Newton CNS

<Small>Developer</Small>

I joined [Newton](https://newton.com/home)
